fastapi==0.111.0 # Ou a versão mais recente que você tinha no seu pip list, como 0.115.12
uvicorn[standard]==0.29.0 # Servidor ASGI para FastAPI
psycopg2-binary==2.9.9 # Para conectar ao PostgreSQL
opencv-python-headless==4.9.0.80 # Para processamento de imagem/vídeo (versão headless é menor)
numpy==1.26.4 # Dependência comum para bibliotecas de ML/Visão
dlib==19.24.4 # Adicionando dlib com versão fixada para reprodutibilidade
face_recognition==1.3.0 # Se você estiver usando esta biblioteca
python-dotenv==1.0.1 # Para carregar .env
pydantic==2.7.1 # Ou a versão que você tinha, como 2.11.4

# Gunicorn pode ser usado em produção com Uvicorn workers, se desejar
# gunicorn==22.0.0